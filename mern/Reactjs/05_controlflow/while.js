// let condition = 0;
// while (condition <= 10) {
//     // code block to be executed

//     console.log("Condition is:", condition);
//     condition++; // Increment the condition to avoid infinite loop
    
// }
// let myArray = ["flash", "samsung", "nokia", "apple", "oneplus"];
// let index = 0;
// while (index < myArray.length) {
//     const element = myArray[index];
//     console.log("Element at index", index, "is", element);
//     index++; // Increment the index to avoid infinite loop
// }
let condition = 0;

do {
    console.log("Condition is:", condition);
    condition++; // Increment the condition to avoid infinite loop
    // The code block will execute at least once even if the condition is false initially
} while (condition <= 10); // The code block will execute at least once even if the condition is false initially